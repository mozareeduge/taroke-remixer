# TAROKE RIMIXER v08 — Hybrid Editor Rebuild Program

This package is the implementation authority for rebuilding the TAROKE RIMIXER editor around the tested v07.8 engine and artifact formats.

Baseline:

- Release tag: `v07.8-release-checkpoint`
- Tested commit: `f7183f01037bf963612d4b56561d3b8cdde306b5`
- Existing executable result: `534 passed, 0 failed`
- Root public application remains v07.8 until v08 cutover.
- The v08 preview is published under `/next/` so the current public work is never replaced by an incomplete rebuild.

The program is one coordinated long-running program composed of bounded work packages, not one undifferentiated coding pass.

Read in numeric order. Claude support templates are under `claude-assets/`.
