// WP04 will implement the full shell: Transport, Navigator, Workspace, Inspector.
// WP05 will wire the complete vertical slice.
// This placeholder establishes the React root.

export function App() {
  return (
    <div style={{ fontFamily: "monospace", padding: "2rem", background: "#000", color: "#fff", minHeight: "100vh" }}>
      <h1>TAROKE RIMIXER v08</h1>
      <p>Editor rebuild in progress — WP04 (UI foundation) not yet started.</p>
      <p>v07 editor available at <a href="/" style={{ color: "#fff" }}>root</a>.</p>
    </div>
  );
}
